<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Taskly</title>
    <link rel="stylesheet" href="/css/output.css" />
    <style>
      body {
        font-family: system-ui, -apple-system, sans-serif;
        background: #f8fafc;
        min-height: 100vh;
        padding: 20px;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <nav class="mb-8">
        <a href="/" class="text-blue-600 hover:text-blue-800">← Назад</a>
      </nav>

      <div class="bg-white rounded-2xl shadow-xl p-8">
        <h1 class="text-3xl font-bold mb-6"><%= title %></h1>
        <div class="prose max-w-none">
          <p class="text-lg mb-4"><%= message %></p>
          <p class="mb-4">
            Это простой проект на Express.js с использованием Tailwind CSS для
            быстрой разработки интерфейсов.
          </p>

          <h2 class="text-2xl font-bold mt-8 mb-4">Используемые технологии:</h2>
          <ul class="list-disc pl-5 mb-6">
            <li>Express.js - backend фреймворк</li>
            <li>Tailwind CSS - утилитарный CSS фреймворк</li>
            <li>EJS - шаблонизатор</li>
            <li>Node.js - среда выполнения</li>
          </ul>

          <div class="mt-8 p-4 bg-blue-50 rounded-lg">
            <h3 class="font-bold mb-2">🚀 Команды для запуска:</h3>
            <code class="bg-gray-800 text-white p-2 rounded block">
              npm install<br />
              npm run tailwind:build<br />
              npm run dev
            </code>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
